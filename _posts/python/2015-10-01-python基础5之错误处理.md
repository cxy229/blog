---
layout     : post
title      : python基础5之错误处理
categories : [python]
tags       : [notes]
---
```python
try:
	a=[0,1]
	#print (a[1])
	#raise IndexError("error") #生成error
	assert a[1]==0,"assert error" #assert不满足时，生成assert error
except Exception as e:
	print ("except excute:",end='')
	print (e)
finally:
	print ("finally excute")
print('end')
```